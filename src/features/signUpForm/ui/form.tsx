import { FC } from 'react'
import { StyleSheet, View } from 'react-native'

import { SignUpCard } from 'entities'

import { Background, Input } from 'shared'

export const SignUpForm: FC = () => <View>FORM</View>

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
})
